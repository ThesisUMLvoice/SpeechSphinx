#JSGF V1.0;

/**
 * JSGF Grammar for Hello World example
 */

grammar hello;

public <greet> = (Name) (Car | Bank);

public <command> = (Create) (Class | Edge | Package);

public <remove> = (Delete) (Class) (Car | Bank);

public <selection> = (Choose) (Draw | Select | Connect | Move | Recognize);

public <edit> = (Redo | Undo);

public <banan> = (Kevin) (Is) (Gay);